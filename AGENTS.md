# AGENTS.md

## 任务完成要求

- 在任务被视为完成之前，必须根据本轮实际修改范围运行对应验收命令，并在答复中说明结果。
- 不要随意运行全量测试；开发或修复功能后，优先运行受影响模块、文件或功能域的定向测试，并在答复中说明覆盖范围。
- 修改应用代码、构建配置、Electron 配置、前端页面、公共类型或核心业务逻辑时，应优先运行相关 lint、typecheck、单元测试或更小范围的验收命令。
- 只有当改动触及共享基础逻辑、跨模块契约、构建配置、打包配置，或用户明确要求完整验收时，才运行全量测试、完整 lint、完整 typecheck 或完整 build。
- 仅修改文档、说明或不会影响运行时行为的配置时，运行与文件类型对应的轻量验收命令；如项目提供编码检查命令，应至少运行编码检查。
- 如果对应验收命令因既有无关问题失败，必须在答复中明确失败命令、失败位置、失败原因，以及是否与本轮修改相关。
- 如果因为环境缺失、依赖未安装、系统限制或命令不存在而无法验收，必须明确说明没有完成验收，并说明阻塞原因。

## 工作原则

- 长期可维护性优先于局部省事。
- 新增功能前，先检查项目中是否已有可复用的模块、组件、类型或工具函数。
- 避免在多个文件重复实现同一逻辑。
- 不要为了赶进度使用局部补丁式绕过问题，优先修正根因。
- 尽量失败即停，不要写不必要的回退逻辑。
- 业务层不要吞错；让异常尽早暴露，必要时只在边界层处理。
- 优先直接调用所属模块或服务，不要无意义地增加中间层。
- 不要在没有明确需求的情况下主动扩展功能范围。
- 不要引入大型依赖来解决小问题；新增依赖前先确认现有技术栈无法合理解决。

## 代码阅读与修改原则

- 修改前先阅读相关文件、类型定义、调用方和已有测试，不要只根据文件名猜测实现。
- 搜索代码时应先明确搜索范围，优先限制在当前项目相关目录内，避免命中无关文档、构建产物、依赖目录或其他工作区。
- 查代码、文件内容或目录结构时，优先使用终端工具，例如 `rg`、`ls`、`Get-Content`、`cat`、`git diff`。
- 不要用浏览器打开本地文件夹、目录索引或 `file://` 页面来阅读代码。
- 修改代码后应检查 `git diff`，确认改动范围符合任务目标。
- 不要顺手修改与当前任务无关的格式、命名、依赖或配置。
- 不要提交临时调试代码、临时日志、无用注释、测试残留文件或本地环境文件。

## 编码约定

- 需要对外暴露的类型、接口、函数和类应补充必要注释。
- 关键分支逻辑应有功能性注释，但不要为显而易见的代码添加噪音注释。
- 写代码时优先复用现有公共组件、类型和工具函数。
- 如果公共逻辑明显可复用，应先抽到合适的公共模块再使用。
- 不要创建本地重复类型；共享契约应放到统一类型文件中。
- 不要为了绕过 TypeScript 问题使用 `any`、随意类型断言或临时替身类型。
- 尽量依赖 TypeScript 推断；只有公共契约、复杂返回值或推断不稳定时才手写显式返回类型。
- 命名应表达业务含义，不要使用模糊名称，例如 `data2`、`temp`、`handleSomething`。
- 不要在源码中硬编码用户本地路径、绝对路径、临时目录、认证信息或机器相关配置。

## TypeScript 约束

- 公共类型应保持单一来源，避免主进程、preload、渲染进程或不同模块之间重复定义同一语义的类型。
- 修改公共类型时，必须检查所有调用方。
- 不要通过降低类型严格性来掩盖真实问题。
- 不要把运行时数据结构和 UI 临时状态混为同一个类型。
- 对外暴露的 API 类型应清晰表达输入、输出和错误边界。
- 能通过类型收敛解决的问题，不要通过运行时字符串判断绕过。

## React 与状态管理

- React 页面状态只应维护页面展示和用户交互需要的数据，不要维护与核心业务冲突的第二份真相。
- 避免 React 最大更新深度和 `useSyncExternalStore` 循环。
- 在 `useEffect` 中写回状态前，应先判断值是否真的变化。
- 对对象和数组比较时，优先按 id、长度或关键字段判断，避免每次渲染都触发 `setState`。
- Zustand 选择器不要返回每次都新建的对象；需要组合多个字段时，使用 `shallow` 或拆成多个选择器。
- store 的 set 和更新函数在没有变化时应返回旧 state 或 prev，避免订阅者反复触发更新。
- 不要为了方便在多个 store 中重复维护同一份业务真相。

## UI 与前端约束

- 前端页面不要放置大段功能说明文字；功能说明应写在文档中，不应出现在应用界面里。
- UI 文案应简洁、明确，优先表达用户当前可以执行的动作。
- 样式实现应优先复用现有组件、设计变量和公共样式。
- 不要在不同页面重复手写同一类按钮、输入框、弹窗或状态展示样式。
- 修改 UI 时，应同时考虑默认状态、加载状态、错误状态、禁用状态和边界输入。
- 不要为了视觉效果破坏可访问性和基本可用性，例如过低对比度、不可聚焦按钮、不可读字号。

## Electron 与桌面应用约束

- Electron 主进程、preload 和渲染进程职责必须清晰。
- 渲染进程不应直接访问 Node.js 能力、文件系统或完整 Electron API。
- preload 只暴露当前功能需要的最小 API。
- 不要关闭上下文隔离。
- 不要启用不必要的 Node 集成。
- 窗口创建、窗口显示、窗口隐藏、托盘、热键、通知等桌面能力应放在主进程侧统一管理。
- 修改窗口生命周期、托盘、热键、通知、打包配置时，应补充相应验收说明。
- 不要把开发环境路径、图标路径、构建输出路径写死到业务代码中。

## 本地存储与配置约束

- 新增本地存储前，先确认数据是否真的需要持久化。
- 持久化数据应有明确的单一来源，不要在多个文件或多个缓存中维护同一份业务真相。
- 配置文件应存放在应用适合的用户数据目录，不要写入源码目录。
- 读写配置时应处理文件不存在、内容为空、JSON 损坏、字段缺失等情况。
- 不要把敏感信息、用户私有数据、本地认证缓存或机器相关配置提交到仓库。
- 如确需新增缓存或派生状态，必须说明源数据、失效条件和写入点。

## 测试要求

- 单元测试应覆盖真实业务行为，不要只写“能通过但证明不了什么”的测试。
- 修复 bug 时，优先补充能证明该 bug 存在且已被修复的测试。
- 单元测试应尽量镜像源码目录结构，每个测试文件聚焦对应源文件或对应模块行为。
- 测试中应避免依赖真实时间、真实网络、真实文件系统或真实系统状态；必要时使用 mock、fake timer 或临时目录。
- 对时间相关逻辑，应优先使用可控时间源或 fake timer，避免测试不稳定。
- 对窗口、托盘、热键、通知等难以自动化的桌面能力，应在答复中说明手动验证范围。
- 不要为了让测试通过而修改测试目标本身，除非需求或接口确实发生变化。

## 验收命令规范

如果项目中存在对应脚本，优先根据改动范围选择以下命令：

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm test:unit
pnpm build
pnpm dist
```

选择原则：

- 只改某个业务模块：优先运行该模块相关单元测试。
- 只改 UI 样式或页面结构：优先运行 lint、typecheck 或前端构建。
- 改公共类型、IPC 契约、共享工具：运行 typecheck，并补充相关测试。
- 改 Electron 主进程、窗口、托盘、热键、通知：运行 typecheck，必要时运行 build，并说明桌面行为验证方式。
- 改构建配置、打包配置、依赖配置：运行 build 或 dist。
- 只改文档：运行项目已有的文档检查、格式检查或编码检查；如果没有对应命令，应至少说明已检查文本内容和编码风险。

答复中必须说明：

- 运行了哪些命令。
- 每个命令是否通过。
- 覆盖了哪些改动范围。
- 未运行哪些理应相关但受限的验收，以及原因。

## 文档要求

- 项目文档必须使用中文书写。
- 技术术语、命令、路径、环境变量、协议名和代码标识可以保留英文。
- 新增或修改文档时，应避免非术语性英文内容与中文随意混写。
- 文档应说明“为什么这样设计”，不要只堆砌操作步骤。
- 文档中涉及命令、路径、配置项时，应保持与项目实际脚本和目录一致。
- 不要在文档中写入未经验证的命令或不存在的脚本。

## 代码与平台注意事项

- 文本文件统一使用 UTF-8 保存。
- 通过脚本或命令写文件时显式指定编码，避免 Windows 默认编码造成乱码。
- 严禁在源码中用 Unicode 转义形式书写常见可见符号，必须直接写可读字符。
- 发现乱码时，先检查文件编码与终端解码设置，必要时重存为 UTF-8。
- Windows 路径处理必须注意反斜杠、空格路径和大小写差异。
- 不要假设开发者使用类 Unix shell；涉及 Windows 命令时应兼容 PowerShell。
- 不要提交构建产物、依赖目录、日志文件、临时文件、系统缓存或认证缓存。

## Git 与提交约束

- 修改前后应查看工作区状态，避免覆盖用户已有改动。
- 不要擅自丢弃、重置、覆盖用户改动。
- 不要在未得到用户要求时主动执行破坏性 Git 操作，例如 `reset --hard`、强制推送、删除分支。
- 提交前应确认 diff 只包含本轮任务相关内容。
- 如果用户要求提交，commit message 应简洁描述本轮实际修改。
- 如果用户没有要求提交，不要默认自动 commit。

## 安全约束

- 不要提交密钥、token、cookie、认证缓存、私有配置或本地账号信息。
- 不要把完整系统能力暴露给前端页面。
- 不要引入会扩大攻击面的依赖或配置。
- 不要关闭安全相关默认配置，除非用户明确要求且已说明风险。
- 处理外部输入、本地配置和 IPC 参数时，应进行必要校验。
- 日志中不要输出敏感路径、用户隐私内容或认证信息。

## 仓库特定约束

- 本项目是 tomato 桌面番茄钟应用，当前阶段优先完成核心计时功能。
- 项目文档中的具体技术选型、架构设计和实现约束，以技术选型与架构设计文档为准；不要在 `AGENTS.md` 中重复展开实现细节。
- 当前阶段不要主动扩展任务管理、学习统计、账号系统、云同步、移动端或 Web 后端服务。
- 产品体验应保持简单、低干扰；新增 UI 和交互时，优先考虑是否符合“轻量桌面工具”的定位。
- 涉及计时、窗口、托盘、热键、通知、设置持久化等核心桌面行为时，必须补充相应测试或验收说明。
- 搜索前必须先明确搜索范围，默认限制在当前仓库相关目录内，避免命中无关文档、构建产物、依赖目录或其他代理工作区。
